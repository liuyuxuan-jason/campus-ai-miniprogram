# 部署与配置指南

> 读完本文档，你就可以在微信开发者工具中运行整个项目了。

---

## 📋 前置条件

| 项目 | 状态 | 操作 |
|------|------|------|
| 微信小程序已注册 | □ | mp.weixin.qq.com → 已拿到 AppID |
| DeepSeek API Key | □ | platform.deepseek.com → 已创建 |
| 微信开发者工具已安装 | □ | 官网下载安装 |
| Node.js 已安装 | □ | 云函数上传需要 |

---

## 🔧 第一步：配置项目

### 1.1 填写 AppID

打开 `project.config.json`，找到 `"appid"` 字段，替换成你的小程序 AppID：

```json
{
  "appid": "wx你的实际AppID"
}
```

### 1.2 填写云开发环境 ID

打开 `miniprogram/app.js`，找到：

```javascript
env: '你的云开发环境ID'
```

替换成你在微信开发者工具中开通的云开发环境 ID（比如 `campus-ai-studio-xxx`）。

---

## ☁️ 第二步：配置云函数

### 2.1 配置 DeepSeek API Key

打开 `cloud-functions/ai-chat/index.js`，找到：

```javascript
const API_KEY = process.env.DEEPSEEK_API_KEY || '你的DeepSeekAPIKey';
```

**推荐做法**：在云函数控制台设置环境变量（更安全）：
1. 在微信开发者工具中右键 `ai-chat` 云函数 → 「上传并部署」
2. 前往微信开发者工具 → 云开发控制台 → 云函数 → `ai-chat` → 版本配置
3. 添加环境变量：`DEEPSEEK_API_KEY` = 你的 DeepSeek API Key

### 2.2 部署所有云函数

在微信开发者工具中：
1. 右键每个云函数目录 → 「上传并部署：云端安装依赖」
2. 等待部署完成（首次安装依赖可能需要 1-2 分钟）

需要部署的云函数：
- ✅ `ai-chat` — AI 对话核心
- ✅ `user-login` — 用户登录
- ✅ `history-manage` — 历史记录管理
- ✅ `user-feedback` — 意见反馈

---

## 🗄️ 第三步：创建数据库集合

在云开发控制台 → 数据库 中创建以下集合：

| 集合名称 | 说明 | 权限设置 |
|----------|------|----------|
| `chat-history` | 对话历史记录 | 所有用户可读，仅创建者可写 |
| `users` | 用户信息 | 所有用户可读，仅创建者可写 |
| `feedback` | 用户反馈 | 仅创建者可写 |

**权限设置步骤**：
1. 进入云开发控制台 → 数据库
2. 点击集合名称 → 「权限设置」
3. 选择「自定义安全规则」
4. 粘贴以下规则：

**chat-history** 规则：
```json
{
  "read": "doc._openid == auth.openid",
  "write": "doc._openid == auth.openid"
}
```

**users** 规则：
```json
{
  "read": "doc._openid == auth.openid",
  "write": "doc._openid == auth.openid"
}
```

**feedback** 规则：
```json
{
  "read": "auth.openid == 'admin'",
  "write": "doc._openid == auth.openid"
}
```

---

## 🎨 第四步：制作 TabBar 图标

TabBar 需要 PNG 格式图片（尺寸：48×48 或 81×81 像素）。

**快速方案**：用电脑自带的画图工具或在线工具制作 6 个简单图标：

| 文件名 | 说明 |
|--------|------|
| `images/home.png` | 首页 - 未选中（灰色） |
| `images/home-active.png` | 首页 - 选中（蓝色） |
| `images/history.png` | 历史 - 未选中（灰色） |
| `images/history-active.png` | 历史 - 选中（蓝色） |
| `images/mine.png` | 我的 - 未选中（灰色） |
| `images/mine-active.png` | 我的 - 选中（蓝色） |
| `images/default-avatar.png` | 默认头像 |

**推荐**：使用 [iconfont.cn](https://www.iconfont.cn/) 下载简单图标，或直接用 PS 做纯色方块+文字。

---

## 🚀 第五步：在开发者工具中打开

1. 打开 **微信开发者工具**
2. 点击「导入项目」
3. 选择项目目录：`campus-ai-studio`（**注意**：选根目录，不是 miniprogram 子目录）
4. AppID 填你的小程序 AppID
5. 后端服务选择「微信云开发」
6. 点击「导入」

---

## 🧪 第六步：测试运行

### 本地测试
1. 在开发者工具中点击「编译」
2. 首页应该正常显示四大功能卡片
3. 点击任一卡片进入对话页面
4. 输入内容发送，检查 AI 是否回复

### 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| TabBar 不显示图标 | 图标不是 PNG 格式或路径不对 | 确认 images/ 下有正确的 png 文件 |
| 云函数调用失败 | 未上传部署 | 右键云函数 → 上传并部署 |
| AI 不回复 | API Key 未配置 | 检查环境变量或代码中的 API Key |
| 登录失败 | AppID 或云环境未配 | 确认 project.config.json 和 app.js |
| 白屏/页面空白 | 编译缓存问题 | 工具 → 清除缓存 → 全部清除 |

---

## 📱 第七步：真机预览

1. 开发者工具点击「预览」
2. 使用微信扫码
3. 在手机上测试所有功能

---

## ✅ 上线检查清单

| 检查项 | 状态 | 
|--------|------|
| AppID 已填写 | □ |
| 云开发环境 ID 已配置 | □ |
| 4 个云函数已上传部署 | □ |
| 3 个数据库集合已创建 | □ |
| DeepSeek API Key 已配置 | □ |
| TabBar 图标已制作 PNG | □ |
| 首页正常显示 | □ |
| 四大模式均可切换 | □ |
| AI 对话正常回复 | □ |
| 历史记录正常保存 | □ |
| 免责声明已展示 | □ |
| 内容安全过滤已启用 | □ |

---

> 📌 **如果部署中遇到任何问题，随时问我！**
