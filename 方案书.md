# 校园AI服务工作室 — 微信小程序 详细方案书

> 版本：v1.0 | 日期：2026-05-23 | 运营主体：个人开发者

---

## 一、项目概述

### 1.1 项目定位
**校园AI服务工作室** 是一款面向高校学生的微信小程序，通过集成大语言模型（LLM）能力，为全校学生提供 AI 写作、AI 学习、AI 创意、AI 对话四大类智能服务，打造校园场景下的"AI 随身助手"。

### 1.2 核心功能一览

| 功能模块 | 具体能力 | 技术实现方式 |
|----------|----------|-------------|
| 📝 AI 写作助手 | 论文润色、作文批改、文案生成 | LLM + 专业提示词模板 |
| 📚 AI 学习助手 | 知识点答疑、笔记总结、资料分析 | LLM + RAG（文件上传） |
| 🎨 AI 创意生成 | PPT 大纲、图片生成、视频脚本 | LLM + 文生图 API |
| 💬 AI 对话助手 | 校园咨询、选课建议、生活助手 | LLM + 校园知识库 RAG |

### 1.3 目标用户
- **范围**：全校在读学生（本科生 + 研究生）
- **用户画像**：
  - 需要写论文/报告的学生（高频）
  - 需要考前复习/知识点整理的学生（中频）
  - 需要制作 PPT/创意内容的学生（中频）
  - 需要校园生活帮助的新生（低频但刚需）

---

## 二、可行性分析

### 2.1 个人注册微信小程序的可行性

**个人主体注册要求：**
- 年满 18 周岁，持有中国大陆身份证
- 一个身份证最多注册 5 个小程序
- 个人主体不需要营业执照
- 不需要企业认证费（企业主体需 300 元/年）

**个人主体限制（需注意）：**
- ❌ **无法开通微信支付**（需要企业/个体工商户主体）
- ❌ 无法使用部分高级接口（如附近的小程序）
- ✅ 可以使用微信云开发
- ✅ 可以使用微信登录、分享等基础能力
- ✅ 可以正常上线和运营

> **结论：个人主体完全可行，但建议后续注册个体工商户（免费）以解锁支付能力。**

### 2.2 技术可行性

#### 技术栈选型

```
┌──────────────────────────────────────┐
│           微信小程序前端                │
│  原生框架 / WXML + WXSS + JS/TS      │
├──────────────────────────────────────┤
│         微信云开发 (Cloud Base)        │
│  ┌──────────┐  ┌──────────┐          │
│  │ 云函数    │  │ 云数据库  │          │
│  │(Node.js) │  │(MongoDB) │          │
│  └──────────┘  └──────────┘          │
│  ┌──────────┐  ┌──────────┐          │
│  │ 云存储    │  │ 云调用    │          │
│  └──────────┘  └──────────┘          │
├──────────────────────────────────────┤
│            AI API 层                   │
│  ┌────────────┐  ┌──────────────────┐│
│  │ DeepSeek   │  │ 通义万相/         ││
│  │ Chat API   │  │ DALL-E API       ││
│  └────────────┘  └──────────────────┘│
└──────────────────────────────────────┘
```

| 层级 | 技术选型 | 选择理由 |
|------|----------|----------|
| **前端** | 微信原生框架 | 原生开发兼容性最好，API 调用最方便 |
| **后端** | 微信云开发 (Cloud Functions) | 无需运维服务器，免费额度够用，与小程序无缝集成 |
| **数据库** | 云数据库（MongoDB-like） | 自带于云开发，自动扩缩容 |
| **AI 对话** | DeepSeek API | ⭐ **核心推荐**：¥1/百万 token，性价比极高，中文能力强 |
| **图片生成** | 通义万相 / Stable Diffusion API | 个人开发者最优选择 |

#### 为什么选 DeepSeek API？

| 对比项 | DeepSeek | OpenAI (GPT-4o) | 通义千问 |
|--------|----------|----------------|---------|
| 价格 | **¥1/百万 tokens** | ~¥60/百万 tokens | ¥2-8/百万 tokens |
| 中文能力 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| 上下文长度 | 128K | 128K | 128K |
| 开发门槛 | 低（HTTP API） | 低 | 低 |
| 注册要求 | 国内手机号 | 需海外支付 | 需阿里云账号 |

> 对于校园场景，DeepSeek 是 **性价比最优解**，覆盖写作、学习、创意、对话四大场景。

### 2.3 核心架构逻辑

所有功能模块本质上是 **同一个 AI 引擎 + 不同的提示词模板**：

```
用户选择功能 → 调用对应云函数 → 组装系统提示词 + 用户输入 
    → DeepSeek API → 返回结果 → 展示给用户
```

**关键优势**：一套架构覆盖所有功能，开发效率极高，维护成本低。

---

## 三、详细功能设计

### 3.1 📝 AI 写作助手

| 子功能 | 能力说明 | 优先级 |
|--------|----------|--------|
| 论文润色 | 语法修正、句式优化、学术化表达、查重降重 | P0 |
| 作文批改 | 评分、语法纠错、结构评价、改进建议 | P1 |
| 文案生成 | 文案创作、内容扩写、段落重写 | P0 |
| 摘要提取 | 长文摘要、文献精简 | P1 |

### 3.2 📚 AI 学习助手

| 子功能 | 能力说明 | 优先级 |
|--------|----------|--------|
| 知识点答疑 | 学科问题回答、解题思路 | P0 |
| 笔记总结 | 上传笔记/教材，自动生成摘要 | P1 |
| 资料分析 | 上传 PDF/图片，智能提取关键信息 | P1 |
| 错题解析 | 拍照/输入错题，分析知识点盲区 | P2 |

### 3.3 🎨 AI 创意生成

| 子功能 | 能力说明 | 优先级 |
|--------|----------|--------|
| PPT 大纲 | 输入主题，生成 PPT 大纲和内容要点 | P0 |
| 图片生成 | 文字描述 → AI 图片 | P1 |
| 视频脚本 | 短视频/课程视频脚本生成 | P1 |
| 活动方案 | 校园活动策划方案生成 | P2 |

### 3.4 💬 AI 对话助手

| 子功能 | 能力说明 | 优先级 |
|--------|----------|--------|
| 智能问答 | 自然语言对话，回答各类问题 | P0 |
| 校园咨询 | 选课建议、培养方案解读、教务信息查询 | P1 |
| 生活助手 | 周边推荐、社团活动查询、校园地图 | P2 |
| 连续对话 | 上下文记忆，多轮对话能力 | P0 |

---

## 四、技术方案详细设计

### 4.1 小程序前端结构

```
pages/
├── index/               # 首页 - 功能入口
│   └── index.wxml
├── chat/                # 统一对话页面（核心页面）
│   └── chat.wxml
├── history/             # 历史记录
│   └── history.wxml
├── mine/                # 个人中心
│   └── mine.wxml
├── feedback/            # 意见反馈
│   └── feedback.wxml
└── about/               # 关于
    └── about.wxml

components/
├── ai-input/            # AI 输入组件（支持文本 + 图片 + 文件）
├── message-bubble/      # 消息气泡组件
├── function-card/       # 功能卡片组件
├── mode-switcher/       # 模式切换组件
└── loading-ai/          # AI 思考加载动画

utils/
├── api.js               # 云函数调用封装
├── prompt-templates.js  # 各功能的提示词模板
├── storage.js           # 本地存储工具
└── constants.js         # 常量定义
```

### 4.2 云函数设计

```
cloud-functions/
├── ai-chat/             # 核心 AI 对话（统一入口）
│   └── index.js         # 接收参数决定模式，调用 DeepSeek API
├── user-login/          # 用户登录
│   └── index.js
├── feedback/            # 意见反馈
│   └── index.js
└── history-manage/      # 历史记录管理
    └── index.js
```

### 4.3 核心逻辑：统一 AI 对话云函数

```javascript
// ai-chat/index.js 核心逻辑
exports.main = async (event) => {
  const { mode, content, fileList, history } = event;
  
  // 1. 根据 mode 获取系统提示词
  const systemPrompt = getSystemPrompt(mode);
  
  // 2. 如果有文件上传，先处理文件内容
  const fileContent = fileList ? await processFiles(fileList) : '';
  
  // 3. 组装消息
  const messages = [
    { role: 'system', content: systemPrompt },
    ...history,  // 历史对话
    { role: 'user', content: fileContent + '\n' + content }
  ];
  
  // 4. 调用 DeepSeek API
  const result = await callDeepSeek(messages);
  
  // 5. 返回结果
  return { reply: result };
};
```

### 4.4 提示词模板管理

```javascript
// prompt-templates.js
const PROMPTS = {
  // 论文润色
  paperPolish: `你是一个专业的学术写作助手。请帮助用户润色以下文本：
- 保持学术严谨性
- 优化语法和句式
- 提升表达的流畅度和专业性
- 不改变原文的核心意思
- 用中文回复`,

  // 知识点答疑
  knowledgeQA: `你是一个大学课程辅导助手。请回答以下问题：
- 解释要清晰准确，适合大学生理解
- 如有必要可以用例子说明
- 给出关键概念的定义和联系
- 不确定的地方要说明`,

  // PPT 大纲
  pptOutline: `你是一个 PPT 大纲助手。请根据用户输入的主题生成 PPT 大纲：
- 包含封面、目录、各章节标题和要点
- 每页控制在 3-5 个要点
- 建议配图方向
- 格式清晰易读`,
  
  // ... 更多模式
};
```

---

## 五、实施计划

### 5.1 总体排期

> 目标：最快 3-4 天 MVP 上线，7 天内完成全部核心功能

```
Day 1 ─ 微信小程序注册 + 云开发环境搭建 + DeepSeek API 申请
Day 2 ─ 前端框架搭建 + 首页 + 统一对话页面
Day 3 ─ 云函数核心逻辑 + DeepSeek 接入 + 基础对话能力
Day 4 ─ 四大模式提示词工程 + 模式切换
Day 5 ─ 文件上传功能 + 历史记录
Day 6 ─ 图片生成接入 + 页面美化 + 用户体验优化
Day 7 ─ 测试 + 审核提交 + 上线
```

### 5.2 上线标准（MVP 定义）

**MVP 必须完成：**
- ✅ 微信登录
- ✅ 首页功能入口（四大模块）
- ✅ 统一对话界面（支持文本输入）
- ✅ 四种模式的提示词切换
- ✅ 基础对话历史保存
- ✅ 样式基本美观

**MVP 可不做（后续迭代）：**
- ❌ 文件上传（PDF/图片）
- ❌ 图片生成功能
- ❌ 校园知识库 RAG
- ❌ 富文本渲染（Markdown 够用）

---

## 六、成本预算

### 6.1 一次性成本

| 项目 | 费用 | 备注 |
|------|------|------|
| 微信小程序注册 | **免费** | 个人主体 |
| 微信云开发开通 | **免费** | 免费基础版 |
| DeepSeek API 注册 | **免费** | 注册送 ¥10 额度 |
| **合计** | **¥0** | 启动成本为 0 |

### 6.2 运营成本（预估）

| 项目 | 预估月费 | 说明 |
|------|----------|------|
| DeepSeek API 调用 | ¥5-30 | 按用量，初期预估低 |
| 云开发资源 | ¥0-10 | 免费额度足够初期使用 |
| 图片生成 API | ¥0-20 | 按需使用 |
| **合计（最低）** | **¥5-60/月** | 个人负担极低 |

### 6.3 DeepSeek 用量估算

- 每次对话平均消耗：300-800 tokens
- 假设日活 100 人，每人 10 次对话
- 月消耗：100×10×500×30 = 1500 万 tokens
- **月费用：约 ¥15**（¥1/百万 tokens）

> 初期几乎零成本运营，随用户增长成本也极低。

---

## 七、个人开发者注册指南

### 7.1 微信小程序注册流程

1. **打开 [微信公众平台](https://mp.weixin.qq.com/)**
2. **点击「立即注册」→ 选择「小程序」**
3. **填写信息：** 邮箱、密码、激活码（发到邮箱）
4. **选择主体类型：** 个人
5. **完善信息：** 姓名、身份证号、手机号
6. **扫码验证：** 管理员微信号扫码绑定
7. **完成注册**

### 7.2 微信云开发开通

1. 在微信开发者工具中打开小程序项目
2. 点击左侧「云开发」图标
3. 点击「开通」→ 同意协议
4. 选择环境名称（如：campus-ai-studio）
5. 等待创建完成

### 7.3 DeepSeek API 申请

1. 访问 [DeepSeek 开放平台](https://platform.deepseek.com/)
2. 注册账号（手机号）
3. 登录后进入 API Keys 页面
4. 创建 API Key
5. 保存 API Key（后续配置到云函数环境变量）

---

## 八、风险与对策

| 风险 | 概率 | 影响 | 对策 |
|------|------|------|------|
| 小程序审核不通过 | 中 | 高 | 提前阅读审核规范，避免敏感词，功能分类明确 |
| DeepSeek API 不稳定 | 低 | 中 | 配置备用 API（通义千问），自动 fallback |
| API 费用超预期 | 低 | 低 | 设置用户每日配额，限制单个请求最大 token 数 |
| 内容安全风险 | 中 | 高 | 添加违禁词过滤，AI 回复增加免责声明 |
| 个人身份限制功能 | 中 | 低 | 后续注册个体工商户（免费）升级主体 |
| 用户增长太快 | 低 | 中 | 初期限制日活上限，云开发自动扩缩容 |

---

## 九、命名建议

既然还没想好名字，这里推荐几个方向：

### 已确认名称
> ✅ **紫荆智学AI** — 紫荆花寓意校园，智学体现 AI 学习属性，独一无二不重名。

### 其他参考（备用）
- **灵犀AI** — 心有灵犀
- **AI 学苑** — AI 学习乐园

---

## 十、下一步行动

1. ✅ **确认本方案** — 阅读后给我反馈或修改建议
2. 📄 **查看 PRD** — 已同步编写开发需求文档
3. 📋 **开始注册** — 立即注册微信小程序和 DeepSeek API
4. 💻 **启动开发** — 方案确认后即可开始编码

---

*本方案基于个人开发者视角编写，所有技术选型优先考虑低成本、高效率、易落地。*
